function minSubarray(nums, k) {
  let res = Number.MAX_SAFE_INTEGER;
  let left = 0;
  let sum = 0;

  for (let i = 0; i < nums.length; i++) {
    sum += nums[i];
    while (sum >= k) {
      re;
    }
  }
}
